@AGENTS.md

## Claude Code

- Use this file as the Claude Code entry point and keep shared guidance in `AGENTS.md`.
- Keep Claude-specific additions here only when they cannot be expressed in the shared guidance.
- For large tasks, summarize the intended approach before editing and keep context focused on the relevant files.
- Note: `ai-assisted-dev/CLAUDE.md` is a *different* file with a different job — it holds personal defaults meant to be imported into `~/.claude/CLAUDE.md` so they apply across every project on this machine, not just this repo. This file governs work *inside* dev-lab; that one travels with me everywhere else. Don't conflate the two.
